mixin pagination()
	- var pagClass = ['pagination text-center']

	+b.DIV(class= pagClass)
		+e.SPAN(title="Предыдущая страница").left &laquo;
		
		+e.SPAN.active 1
		- for ( var i = 2; i <= 5; i++ )
			+e.A(href="#").link= i
		
		+e.A(href= "#" title="Следущая страница").right &raquo;